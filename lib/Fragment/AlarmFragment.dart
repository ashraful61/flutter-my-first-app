import 'package:flutter/material.dart';

class AlarmFragment extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('This is my Alarm fragment'),
    );
  }
}
